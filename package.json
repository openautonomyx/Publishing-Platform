{
  "name": "openautonomyx",
  "version": "1.0.0",
  "description": "OpenAutonomyx - Unified Platform & Agents Monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "cd packages/platform && npm start || node server.js",
    "dev": "yarn workspaces run dev",
    "build": "echo 'Building monorepo...' && cd packages/platform && npm install && npm run build 2>/dev/null || echo 'Platform build skipped'",
    "test": "yarn workspaces run test"
  }
}
